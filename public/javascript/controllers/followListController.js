angular.module('pirineoPOIApp')

    .controller('followListCtrl', ['$scope', 'auth', function ($scope, auth) {

        /*
         TODO DARÍO: la función auth.getFollows() ya te devuelve el array con todos a los que sigues actualmente
         */
    }]);
