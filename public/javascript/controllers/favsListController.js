angular.module('pirineoPOIApp')

    .controller('favsListCtrl', ['$scope', 'auth', function ($scope, auth) {

        /*
        TODO DARÍO: la función auth.getFavs() ya te devuelve el array con todos los favoritos actuales
         */
    }]);
